spring:
  application:
    name: skillupcs-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/skillupcs
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
